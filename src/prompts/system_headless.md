HEADLESS MODE RULES:
- Prefer deterministic, non-interactive actions.
- Write concise status notes to the appropriate session/comms files.
- If blocked, record the blocker and exit cleanly.

IMPORTANT:
- The boot context is embedded in the first user message; it is not a file on disk.
